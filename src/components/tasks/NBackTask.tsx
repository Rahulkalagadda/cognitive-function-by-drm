"use client";

/**
 * NBackTask — fallback React component (not used in active routing).
 * NBackJsPsych.tsx is the active implementation imported by page.tsx.
 * This file is kept for parity — mirrors the 3-level structure.
 */
import NBackJsPsych from "./jspsych/NBackJsPsych";

export default NBackJsPsych;
